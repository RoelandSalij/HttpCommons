// This file was generated by Mendix Modeler.
//
// WARNING: Only the following code will be retained when actions are regenerated:
// - the import list
// - the code between BEGIN USER CODE and END USER CODE
// - the code between BEGIN EXTRA CODE and END EXTRA CODE
// Other code you write will be lost the next time you deploy the project.
// Special characters, e.g., é, ö, à, etc. are supported in comments.

package httpcommons.actions;

import com.mendix.m2ee.api.IMxRuntimeResponse;
import com.mendix.systemwideinterfaces.core.IContext;
import com.mendix.webui.CustomJavaAction;

public class SetCookie extends CustomJavaAction<java.lang.Boolean>
{
	private java.lang.String cookiename;
	private java.lang.String content;

	public SetCookie(IContext context, java.lang.String cookiename, java.lang.String content)
	{
		super(context);
		this.cookiename = cookiename;
		this.content = content;
	}

	@Override
	public java.lang.Boolean executeAction() throws Exception
	{
		// BEGIN USER CODE
		IContext ctx = getContext();
		IMxRuntimeResponse resp = ctx.getRuntimeResponse().get();
		resp.addCookie(cookiename, content);
		return true;
		// END USER CODE
	}

	/**
	 * Returns a string representation of this action
	 */
	@Override
	public java.lang.String toString()
	{
		return "SetCookie";
	}

	// BEGIN EXTRA CODE
	// END EXTRA CODE
}
